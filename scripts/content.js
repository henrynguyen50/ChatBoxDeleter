//script to delete chat box

//just for streamed.su first test pogchamp
const element = document.querySelectorAll(".w-full h-auto border-border/40 border-2 rounded-lg drop-shadow-lg p-2 relative")
if (element) {
    element.forEach(el => el.remove());
}